[package]
name = "accumulator"
version = "6.0.0-beta"
authors = ["Use Ink <ink@use.ink>"]
edition = "2024"

[dependencies]
ink = { version = "6.0.0-beta", default-features = false }

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "ink/std",
]
ink-as-dependency = []

[package.metadata.ink-lang]
abi = "ink"
